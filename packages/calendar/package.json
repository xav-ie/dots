{
  "name": "calendar",
  "private": true,
  "type": "module",
  "description": "Type-resolution manifest for tsgo only; the app is built by `ags bundle` (see default.nix), not npm. `ags` is not on npm — it's symlinked into node_modules from the flake input by the `just tc` recipe.",
  "dependencies": {
    "gnim": "^1.9.1"
  },
  "devDependencies": {
    "@ts-for-gir/cli": "^4.0.4"
  }
}
